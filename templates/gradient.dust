background-image: {#gradient}{prefix}{type}-gradient({@if cond="'{prefix}'.length > 0"}{angle|gradientAngle}{:else}{angle|css3GradientAngle}{/if}
{#stops}
    , {@color}{color}{/color} {location}%
{/stops}
){@sep}, {/sep}{/gradient};{~n}
